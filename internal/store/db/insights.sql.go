// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: insights.sql

package db

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const createInsightRun = `-- name: CreateInsightRun :one

INSERT INTO insight_runs (survey_id, response_watermark, response_count, model, output, created_at)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, survey_id, response_watermark, response_count, model, output, created_at
`

type CreateInsightRunParams struct {
	SurveyID          uuid.UUID  `json:"survey_id"`
	ResponseWatermark *time.Time `json:"response_watermark"`
	ResponseCount     int32      `json:"response_count"`
	Model             string     `json:"model"`
	Output            string     `json:"output"`
	CreatedAt         time.Time  `json:"created_at"`
}

// M10: Insight Summaries. Append-only by trigger; the newest run for a
// survey is the one shown, and its watermark is the cache key.
func (q *Queries) CreateInsightRun(ctx context.Context, arg CreateInsightRunParams) (InsightRun, error) {
	row := q.db.QueryRow(ctx, createInsightRun,
		arg.SurveyID,
		arg.ResponseWatermark,
		arg.ResponseCount,
		arg.Model,
		arg.Output,
		arg.CreatedAt,
	)
	var i InsightRun
	err := row.Scan(
		&i.ID,
		&i.SurveyID,
		&i.ResponseWatermark,
		&i.ResponseCount,
		&i.Model,
		&i.Output,
		&i.CreatedAt,
	)
	return i, err
}

const latestInsightRun = `-- name: LatestInsightRun :one
SELECT id, survey_id, response_watermark, response_count, model, output, created_at
FROM insight_runs
WHERE survey_id = $1
ORDER BY created_at DESC
LIMIT 1
`

func (q *Queries) LatestInsightRun(ctx context.Context, surveyID uuid.UUID) (InsightRun, error) {
	row := q.db.QueryRow(ctx, latestInsightRun, surveyID)
	var i InsightRun
	err := row.Scan(
		&i.ID,
		&i.SurveyID,
		&i.ResponseWatermark,
		&i.ResponseCount,
		&i.Model,
		&i.Output,
		&i.CreatedAt,
	)
	return i, err
}
